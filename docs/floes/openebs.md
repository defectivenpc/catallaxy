# openebs

Rancher's local-path provisioner as the cluster's default StorageClass.

<!-- Generated by nix/floe-docs.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| chart | string | *(required)* | Store path of the local-path-provisioner Helm chart. Required. |
| className | string | `"local-path"` | Name of the StorageClass it creates. |
| namespace | string | `"openebs"` | Namespace the provisioner runs in. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| storageClass | STORAGE_CLASS | A StorageClass a PVC can name, and whether it is the cluster's default. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| openebs | — | condition | localPathProvisioner |

## Provides, as linked

### `storageClass` — STORAGE_CLASS

| field | value |  |
| --- | --- | --- |
| className | `"local-path"` | link-local |
| isDefault | `true` | link-local |

