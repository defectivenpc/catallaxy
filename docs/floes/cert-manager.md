# cert-manager

cert-manager, and an issuer for the lab's own certificates.

<!-- Generated by nix/floe-docs.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| caCommonName | string | `"catallaxy lab CA"` | Subject common name on the root certificate. |
| chart | string | *(required)* | Store path of the cert-manager Helm chart. Required. |
| issuerName | string | `"lab-ca"` | Name of the ClusterIssuer signing from the lab's own CA. |
| namespace | string | `"cert-manager"` | Namespace the controller and webhook run in. |
| rootFromLab | boolean | `false` | Take the root from the lab instead of minting one.  A lab that runs the host ingress already has a root: `cert-generate` mints it, HAProxy serves from it, and `import_lab_ca` seeds it into this namespace as the issuer's backing Secret before any manifest is applied. Minting a second one would overwrite that Secret, and the two halves of the lab would then present certificates from different chains — a client trusting one rejecting the other, with nothing failing loudly enough to say why.  With this set the floe drops its `SelfSigned` bootstrap issuer and its root `Certificate`, and keeps only the `CA` ClusterIssuer that reads the Secret. Set it exactly when `lab.proxy.tls.enable` is on.  |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| issuance | X509_ISSUANCE | Something that signs certificates, and whether a browser will trust what it signs. |
| webhook | X509_WEBHOOK | The certificate controller's admission webhook is serving, so its CRs are accepted. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| cert-manager | — | condition | cainjector, controller, startupapicheck, webhook |
| issuers | cert-manager | condition | — |

## Provides, as linked

### `issuance` — X509_ISSUANCE

| field | value |  |
| --- | --- | --- |
| caSecret | `{"key":"tls.crt","name":"lab-ca-ca-secret","namespace":"cert-manager"}` | link-local |
| issuerRef | `{"kind":"ClusterIssuer","name":"lab-ca"}` | link-local |
| publicIssuer | `false` | travels |

### `webhook` — X509_WEBHOOK

| field | value |  |
| --- | --- | --- |
| crdKinds | `["cert-manager.io/Certificate","cert-manager.io/Issuer","cert-manager.io/ClusterIssuer","cert-manager.io/CertificateRequest","acme.cert-manager.io/Challenge","acme.cert-manager.io/Order"]` | link-local |
| namespace | `"cert-manager"` | link-local |


## Verify checks

| check |
| --- |
| issuers/issuers-ready |
