# loki

Loki, the log store.

<!-- Generated by nix/floe-docs.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| chart | string | *(required)* | Store path of the Loki Helm chart. Required. |
| namespace | string | `"loki"` | Namespace Loki runs in. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| logs | LOG_INGEST | Where logs are pushed and queried. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| loki | — | condition | canary, gateway, loki, sidecar |

## Provides, as linked

### `logs` — LOG_INGEST

| field | value |  |
| --- | --- | --- |
| otlpUrl | `"http://loki.loki.svc.cluster.local:3100/otlp"` | link-local |
| pushUrl | `"http://loki.loki.svc.cluster.local:3100/loki/api/v1/push"` | link-local |
| queryUrl | `"http://loki.loki.svc.cluster.local:3100"` | link-local |

