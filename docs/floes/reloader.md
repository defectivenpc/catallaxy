# reloader

Reloader, which restarts a workload when a Secret or ConfigMap it mounts changes.

<!-- Generated by nix/floe-interface.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| chart | string | *(required)* | Store path of the Reloader Helm chart. Required. |
| namespace | string | `"reloader"` | Namespace the controller runs in. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| reload | CONFIG_RELOAD | The annotations that make a workload restart when a Secret or ConfigMap it mounts changes. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| reloader | — | condition | controller |

## Provides, as linked

### `reload` — CONFIG_RELOAD

| field | value |  |
| --- | --- | --- |
| configMapAnnotation | `"configmap.reloader.stakater.com/reload"` | link-local |
| secretAnnotation | `"secret.reloader.stakater.com/reload"` | link-local |

