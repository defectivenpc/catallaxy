# harbor

Harbor, an OCI registry with its own database and cache.

<!-- Generated by nix/floe-docs.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| chart | string | *(required)* | Store path of the Harbor Helm chart. Required. |
| namespace | string | `"harbor"` | Namespace Harbor runs in. |
| oidc | boolean | `false` | Register an OAuth2 client so Harbor can authenticate against the lab's issuer.  Harbor's own OIDC settings are applied through its API rather than its chart, so this renders the client and lands the credentials; wiring them into Harbor's auth mode is a `cata lab ops` step, not a value.  |
| storage | string | `"10Gi"` | Size of the volume the registry's blobs live on. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |
| gateway | API_GATEWAY | An ingress a workload attaches an HTTPRoute to, and the zone its hostnames live in. |
| generation | SECRET_GENERATION | A controller that mints secret values in the cluster, so none is rendered into a manifest. |

## Requires, optionally

| hole | signature | what it is |
| --- | --- | --- |
| oidc | OIDC_PROVIDER | An OIDC issuer, its endpoints, and the CR a consumer registers a client with. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| registry | OCI_REGISTRY | A container registry, addressed for a human and for a node pulling an image. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| harbor | — | condition | core, database, jobservice, nginx, portal, redis, registry, registryctl |

## Provides, as linked

### `registry` — OCI_REGISTRY

| field | value |  |
| --- | --- | --- |
| credentials | `{"name":"harbor-admin","namespace":"harbor","passwordKey":"HARBOR_ADMIN_PASSWORD","usernameKey":"harbor-user"}` | link-local |
| namespace | `"harbor"` | link-local |
| pullRef | `"harbor.every-floe.test"` | link-local |
| url | `"https://harbor.every-floe.test"` | travels |

