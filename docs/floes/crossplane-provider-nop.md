# crossplane-provider-nop

The nop Crossplane provider, whose managed resources reconcile without touching anything.

<!-- Generated by nix/floe-docs.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| name | string | `"provider-nop"` | Name of the Provider object, and the prefix of the pod it runs. |
| package | string | `"ghcr.io/crossplane-contrib/provider-nop:v0.4.0"` | The provider package. Crossplane pulls this itself rather than the applier doing it, which is why it is declared in `images` even though no rendered pod spec names it.  `ghcr.io` rather than `xpkg.upbound.io`: both publish this package at the same digest, and the lab's pull-through cache already mirrors the first. A lab whose nodes resolve through its own DNS cannot reach an unmirrored registry at all — that server is authoritative for the zone and answers REFUSED for everything else.  |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |
| controlPlane | MANAGED_RESOURCE_CONTROL_PLANE | A control plane that installs providers and reconciles the resources they define. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| resourceProvider | MANAGED_RESOURCE_PROVIDER | A provider is healthy and the resource kinds it defines can be applied. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| provider | — | condition | provider |

## Provides, as linked

### `resourceProvider` — MANAGED_RESOURCE_PROVIDER

| field | value |  |
| --- | --- | --- |
| crdKinds | `["nop.crossplane.io/NopResource"]` | link-local |
| healthy | `"managed-resources/provider-nop/healthy"` | link-local |

