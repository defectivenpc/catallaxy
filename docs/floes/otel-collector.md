# otel-collector

An OpenTelemetry collector, exporting to whichever backends the cluster has.

<!-- Generated by nix/floe-interface.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| agent | boolean | `true` | Also run a DaemonSet that tails container logs on every node and forwards them to the gateway.  Separate from the gateway because it is the expensive half: it runs once per node, mounts the host's log directory, and a cluster that already ships logs another way wants the gateway without it.  |
| chart | string | *(required)* | Store path of the opentelemetry-collector Helm chart. Required. |
| namespace | string | `"observability"` | Namespace the collector runs in. |
| otlpGrpcPort | 16 bit unsigned integer; between 0 and 65535 (both inclusive) | `4317` | Port the gateway accepts OTLP/gRPC on. |
| otlpHttpPort | 16 bit unsigned integer; between 0 and 65535 (both inclusive) | `4318` | Port the gateway accepts OTLP/HTTP on. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |

## Requires, optionally

| hole | signature | what it is |
| --- | --- | --- |
| logs | LOG_INGEST | Where logs are pushed and queried. |
| metrics | METRICS_INGEST | Where metrics are written and queried, and the monitoring CRDs a consumer may use. |
| traces | TRACE_INGEST | Where traces are sent and queried. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| agent | gateway | — | — |
| gateway | — | condition | collector |
