# secret-store

A ClusterSecretStore pointing at a backend the lab holds.

<!-- Generated by nix/floe-docs.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| caBundle | null or (submodule) | `null` | A CA to verify the store's certificate against.  Needed whenever the lab hosts the store itself: it answers at a lab hostname over TLS the lab's own CA signed, and external-secrets verifies against the pod's trust store, which has never heard of it. Every lab-hosted store failed `InvalidProviderConfig` until something pointed at the CA the cluster already carries. A store elsewhere is signed by a public CA and must keep using the pod's own roots, so this is null by default.  |
| labStore | string | *(required)* | The `lab.secrets.stores` entry this backs. Required.  The rendered object's name is derived from it rather than given, so the lab wiring that references the store and this floe cannot end up naming different things.  |
| path | string | `"secret"` | KV mount path. |
| server | string | *(required)* | Base URL of the vault-compatible server. Required.  Reachable from *every* cluster that uses the store. An in-cluster address resolves only inside the cluster running the store, and a subscriber elsewhere waits forever on something it cannot dial — the lab refuses that combination rather than letting it render.  |
| tokenSecret | submodule | `{}` | Where the store's own credential lives. |
| version | one of "v1", "v2" | `"v2"` | KV engine version. Writing a v2 mount as though it were v1 succeeds and stores the wrong shape, which nothing notices until a reader gets an envelope where it expected a value.  |
| writable | boolean | `true` | Whether a cluster may push into it. False makes this a read-only mirror of values authored elsewhere, and the lab refuses a `publish` naming it.  |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |
| generation | SECRET_GENERATION | A controller that mints secret values in the cluster, so none is rendered into a manifest. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| secretStore | SECRET_STORE | A named store an ExternalSecret reads from, and whether it may be written to. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| store | — | condition | — |

## Provides, as linked

### `secretStore` — SECRET_STORE

| field | value |  |
| --- | --- | --- |
| storeKind | `"ClusterSecretStore"` | link-local |
| storeName | `"catallaxy-runtime"` | link-local |
| writable | `true` | link-local |

