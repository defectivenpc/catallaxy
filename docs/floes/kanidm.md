# kanidm

Kanidm as the lab's OIDC provider, reconciled by kaniop.

<!-- Generated by nix/floe-interface.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| clientsAnyNamespace | boolean | `true` | Reconcile the resources consumers render — OAuth2 clients, service accounts and groups — in any namespace.  On, because a consumer renders its own and a consumer lives in its own namespace. Off, kaniop looks only in this one: a resource elsewhere is admitted, stored, and never reconciled, and the consumer waits for a Secret that is not coming. `kinds.mkOAuth2Client` and `kinds.mkServiceAccount` refuse that combination rather than letting it render.  Named for clients because that is what the `OIDC_PROVIDER` field is called and the signature is contract, but it has never been only about clients — it was only ever *set* for them, which is how netbird's service account came to sit unreconciled with an empty status.  |
| domain | string | *(required)* | DNS name the server answers as. Required.  Not cosmetic: it goes into WebAuthn's relying-party id and into every issued token's issuer claim, so it must match the hostname clients actually reach — a mismatch is a credential that verifies nowhere.  |
| namespace | string | `"kanidm"` | Namespace the server runs in. |
| registry | string | `"docker.io"` | Registry the server image is pulled from. |
| repository | string | `"kanidm/server"` | Repository the server image lives in. |
| storage | string | `"1Gi"` | Size of the volume the database lives on. |
| version | string | `"1.6.4"` | Kanidm version the operator runs, as the tag of `registry/repository`.  Not `spec.version` on the CR: kaniop v1beta1 has no such field, and a `Kanidm` carrying one is refused by the API server with `.spec.version: field not declared in schema` — under server-side apply the whole object is rejected, so the floe installs nothing at all. What the CRD takes is `spec.image`, whose own default is `kanidm/server:latest`, which a lab that means to be reproducible cannot use.  |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |
| gateway | API_GATEWAY | An ingress a workload attaches an HTTPRoute to, and the zone its hostnames live in. |
| issuance | X509_ISSUANCE | Something that signs certificates, and whether a browser will trust what it signs. |
| operator | IDENTITY_OPERATOR | The identity operator's CRDs are established, so its CRs can be applied. |
| trust | TRUST_BUNDLE | A CA bundle distributed into namespaces, for workloads that verify the lab's own certificates. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| oidc | OIDC_PROVIDER | An OIDC issuer, its endpoints, and the CR a consumer registers a client with. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| server | — | condition | server |

## Provides, as linked

### `oidc` — OIDC_PROVIDER

| field | value |  |
| --- | --- | --- |
| authorizationEndpoint | `"https://idm.every-floe.test/ui/oauth2"` | travels |
| clientCrd | `"kaniop.rs/KanidmOAuth2Client"` | link-local |
| clientsAnyNamespace | `true` | link-local |
| issuer | `"https://idm.every-floe.test"` | travels |
| ref | `{"name":"kanidm","namespace":"kanidm"}` | link-local |
| tokenEndpoint | `"https://idm.every-floe.test/oauth2/token"` | travels |

