# gateway-api-crds

The Gateway API CRDs, so Gateway and route kinds have types.

<!-- Generated by nix/floe-interface.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| manifest | string | *(required)* | Store path of the upstream CRD bundle. Required — the caller pins the URL and hash in `lib/k8s-specs.nix`.  |
| version | string | *(required)* | Gateway API release the manifest came from. Required. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| api | GATEWAY_API | The Gateway API CRDs are registered, so Gateway and route kinds have types. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| crds | — | — | — |

## Provides, as linked

### `api` — GATEWAY_API

| field | value |  |
| --- | --- | --- |
| crdKinds | `["kind:gateway.networking.k8s.io/GatewayClass","kind:gateway.networking.k8s.io/Gateway","kind:gateway.networking.k8s.io/ReferenceGrant","kind:gateway.networking.k8s.io/HTTPRoute","kind:gateway.networking.k8s.io/GRPCRoute","kind:gateway.networking.k8s.io/TCPRoute","kind:gateway.networking.k8s.io/TLSRoute","kind:gateway.networking.k8s.io/UDPRoute","kind:gateway.networking.k8s.io/BackendTLSPolicy","kind:gateway.networking.k8s.io/BackendLBPolicy"]` | link-local |
| version | `"v1.2.1"` | link-local |

