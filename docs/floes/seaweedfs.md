# seaweedfs

SeaweedFS, an S3-compatible object store.

<!-- Generated by nix/floe-interface.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| chart | string | *(required)* | Store path of the SeaweedFS Helm chart. Required. |
| namespace | string | `"seaweedfs"` | Namespace it runs in. |
| version | string | `"3.80"` | SeaweedFS image tag. The chart's own default lags the one this pins. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| objectStore | OBJECT_STORE | An S3-compatible endpoint and the credentials, if any, needed to reach it. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| seaweedfs | — | condition | seaweedfs |

## Provides, as linked

### `objectStore` — OBJECT_STORE

| field | value |  |
| --- | --- | --- |
| credentials | `null` | link-local |
| namespace | `"seaweedfs"` | link-local |
| s3Endpoint | `"http://seaweedfs-s3.seaweedfs.svc.cluster.local:8333"` | link-local |

