# zot

Zot, a small OCI registry used as the lab's pull-through cache.

<!-- Generated by nix/floe-docs.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| chart | string | *(required)* | Store path of the zot Helm chart. Required. |
| namespace | string | `"zot"` | Namespace it runs in. |
| port | 16 bit unsigned integer; between 0 and 65535 (both inclusive) | `5000` | Port the registry serves on. |
| storage | string | `"8Gi"` | Size of the volume claim holding the images. |
| storageClass | null or string | `null` | Storage class for that claim. Null takes the cluster's default. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| registry | OCI_REGISTRY | A container registry, addressed for a human and for a node pulling an image. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| zot | — | condition | zot |

## Provides, as linked

### `registry` — OCI_REGISTRY

| field | value |  |
| --- | --- | --- |
| credentials | `null` | link-local |
| namespace | `"zot"` | link-local |
| pullRef | `"zot.zot.svc.cluster.local:5000"` | link-local |
| url | `"http://zot.zot.svc.cluster.local:5000"` | travels |

