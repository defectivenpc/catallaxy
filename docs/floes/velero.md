# velero

Velero, cluster backup and restore against an object store.

<!-- Generated by nix/floe-docs.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| bucket | string | `"velero"` | Bucket backups are written to. It must already exist. |
| chart | string | *(required)* | Store path of the Velero Helm chart. Required. |
| crds | string | *(required)* | Store path of the CRD manifest extracted from the chart. Required.  Their own bundle, because the chart's `installCRDs` runs them through a hook that re-applies on every upgrade, and because a consumer emitting a `Backup` needs the kind to exist without waiting for the controller.  |
| namespace | string | `"velero"` | Namespace the controller runs in. |
| region | string | `"us-east-1"` | Region the S3 client claims.  Meaningless to a self-hosted store and required by the client anyway: the AWS SDK refuses to sign a request without one.  |
| schedules | attribute set of (submodule) | `{}` | Recurring backups, as Velero `Schedule` resources. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |
| objectStore | OBJECT_STORE | An S3-compatible endpoint and the credentials, if any, needed to reach it. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| crds | — | — | — |
| velero | crds | condition | awsPlugin, velero |

## Operator commands

| command | what it does |
| --- | --- |
| `<lab>-ops backup velero-create` | Create a backup |
| `<lab>-ops backup velero-delete` | Delete a backup |
| `<lab>-ops backup velero-describe` | Describe a backup |
| `<lab>-ops backup velero-list` | List backups |
| `<lab>-ops backup velero-restore` | Restore from a backup |
| `<lab>-ops backup velero-schedules` | List backup schedules |
| `<lab>-ops backup velero-trigger` | Run a schedule now |
