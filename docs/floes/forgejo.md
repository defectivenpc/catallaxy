# forgejo

Forgejo, a git server, with an admin account and repositories bootstrapped.

<!-- Generated by nix/floe-docs.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| adminUser | string | `"forgejo-admin"` | Name of the initial admin account.  Not `admin`: Forgejo reserves that name and refuses to create it, failing the chart's init job with a message about a reserved username rather than about the value you set.  |
| chart | string | *(required)* | Store path of the Forgejo Helm chart. Required. |
| namespace | string | `"forgejo"` | Namespace Forgejo runs in. |
| oidc | boolean | `false` | Add the lab's issuer as a login source. |
| repository | string | `"lab"` | Repository the bootstrap Job creates for a CD tool to clone.  `cata`'s `bootstrap-forgejo-repos` step waits for that Job by label, so a lab publishing manifests here needs it to exist and to finish.  |
| storage | string | `"5Gi"` | Size of the volume repositories live on. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |
| gateway | API_GATEWAY | An ingress a workload attaches an HTTPRoute to, and the zone its hostnames live in. |
| generation | SECRET_GENERATION | A controller that mints secret values in the cluster, so none is rendered into a manifest. |

## Requires, optionally

| hole | signature | what it is |
| --- | --- | --- |
| oidc | OIDC_PROVIDER | An OIDC issuer, its endpoints, and the CR a consumer registers a client with. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| git | GIT_REPOSITORY | A git remote, addressed differently from inside and outside the cluster. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| bootstrap | forgejo | — | bootstrap |
| forgejo | — | condition | forgejo |

## Provides, as linked

### `git` — GIT_REPOSITORY

| field | value |  |
| --- | --- | --- |
| cloneUrl | `"https://git.every-floe.test/forgejo-admin/lab.git"` | travels |
| credentials | `{"name":"forgejo-admin","namespace":"forgejo","passwordKey":"password","username":"forgejo-admin","usernameKey":"username"}` | link-local |
| externalUrl | `"https://git.every-floe.test"` | travels |
| internalUrl | `"http://forgejo-http.forgejo.svc.cluster.local:3000"` | link-local |


## Operator commands

| command | what it does |
| --- | --- |
| `<lab>-ops git forgejo-add-login-source` | Register the lab's OIDC issuer as a Forgejo login source |
