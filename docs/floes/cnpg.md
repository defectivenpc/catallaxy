# cnpg

CloudNativePG, the PostgreSQL operator.

<!-- Generated by nix/floe-interface.nix. Refresh: nix run .#refresh-floe-docs -->

## Inputs

| input | type | default | description |
| --- | --- | --- | --- |
| chart | string | *(required)* | Store path of the CloudNativePG Helm chart. Required. |
| namespace | string | `"cnpg-system"` | Namespace the operator runs in. |

## Requires

| hole | signature | what it is |
| --- | --- | --- |
| cluster | KUBERNETES_CLUSTER | The cluster a member installs into: its name, version, context and address ranges. |

## Provides

| promise | signature | what it is |
| --- | --- | --- |
| operator | POSTGRES_OPERATOR | The PostgreSQL operator's CRDs are registered, so a Cluster CR has a type. |

## Emits

| alias | kind | what it carries |
| --- | --- | --- |
| component | catallaxy.component | What a floe installs into a cluster: bundles, and the operator surface around them. |

## Bundles

| bundle | waits for | ready when | images |
| --- | --- | --- | --- |
| cnpg | — | condition | operator |

## Provides, as linked

### `operator` — POSTGRES_OPERATOR

| field | value |  |
| --- | --- | --- |
| crdKinds | `["postgresql.cnpg.io/Cluster","postgresql.cnpg.io/Pooler","postgresql.cnpg.io/ScheduledBackup","postgresql.cnpg.io/Backup"]` | link-local |

