# I/O outside cli/src/io that the scanner can see.
#
# The check fails on anything not listed here AND on any entry that no
# longer fires, so this can only shrink. Delete a line when you fix it.
#
# bin/cata.rs is the exception that stays: the interrupt handler exits
# the process deliberately, which is the whole point of it.
bin/cata.rs:16

# TcpStream and ToSocketAddrs: a real connect and a real resolve, in the
# host service prober.
host/services.rs:5

# `reqwest::Client::builder()` handed to io::http::client, which applies the
# proxy and CA policy. The seam is respected; what leaks is the builder type.
# Narrowing io::http::client to take the two settings these callers actually
# want (timeout, user agent) would close it.
images/mod.rs:250
verify/checks/endpoints.rs:41
